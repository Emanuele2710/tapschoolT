<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.leo.tapschool_gestioneimpegniscolastici">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TipoUtente" />
        <activity android:name=".RegistraStudente" />
        <activity android:name=".Registra_Professore" />
        <activity android:name=".Modifica_Studente" />
        <activity android:name=".InserisciNota" />
        <activity android:name=".Home" />
        <activity android:name=".ModificaNota"></activity>
    </application>

</manifest>